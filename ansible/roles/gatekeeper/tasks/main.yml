---
- name: Validate router Wi-Fi secret is set and not using placeholder values
  ansible.builtin.assert:
    that:
      - wifi_password is defined
      - wifi_password | length >= 8
      - wifi_password not in ['vaulted', 'REPLACE_WITH_REAL_WIFI_PASSWORD']
      - wifi_guest_password is defined
      - wifi_guest_password | length >= 8
      - wifi_guest_password not in ['vaulted_guest', 'REPLACE_WITH_REAL_GUEST_WIFI_PASSWORD']
    fail_msg: >-
      wifi_password or wifi_guest_password is missing or still using a placeholder.
      Edit ansible/group_vars/router.sops.yml with sops and set both fields.
    quiet: true
  no_log: true

- name: Regenerate SSH host keys to ed25519 if not already present
  ansible.builtin.raw: |
    if [ ! -f /etc/dropbear/dropbear_ed25519_host_key ]; then
      dropbearkey -t ed25519 -f /etc/dropbear/dropbear_ed25519_host_key
      /etc/init.d/dropbear restart
    fi
  changed_when: false

- name: Enable software and hardware flow offloading and harden firewall defaults
  ansible.builtin.raw: |
    set -eu
    changed=0

    current_sw="$(uci -q get firewall.@defaults[0].flow_offloading || true)"
    if [ "$current_sw" != "1" ]; then
      uci set firewall.@defaults[0].flow_offloading='1'
      changed=1
    fi

    current_hw="$(uci -q get firewall.@defaults[0].flow_offloading_hw || true)"
    if [ "$current_hw" != "1" ]; then
      uci set firewall.@defaults[0].flow_offloading_hw='1'
      changed=1
    fi

    [ "$(uci -q get firewall.@defaults[0].syn_flood || true)" = "1" ] || \
      { uci set firewall.@defaults[0].syn_flood='1'; changed=1; }

    [ "$(uci -q get firewall.@defaults[0].drop_invalid || true)" = "1" ] || \
      { uci set firewall.@defaults[0].drop_invalid='1'; changed=1; }

    if [ "$changed" -eq 1 ]; then
      echo CHANGED
    else
      echo OK
    fi
  register: gatekeeper_perf
  changed_when: "'CHANGED' in gatekeeper_perf.stdout"

- name: Unify SSID and key across 2.4GHz and 5GHz radios
  ansible.builtin.raw: |
    set -eu
    changed=0

    current_radio0_disabled="$(uci -q get wireless.radio0.disabled || true)"
    if [ "$current_radio0_disabled" != "0" ]; then
      uci set wireless.radio0.disabled='0'
      changed=1
    fi

    current_radio1_disabled="$(uci -q get wireless.radio1.disabled || true)"
    if [ "$current_radio1_disabled" != "0" ]; then
      uci set wireless.radio1.disabled='0'
      changed=1
    fi

    current_channel_5="$(uci -q get wireless.radio1.channel || true)"
    if [ "$current_channel_5" != "{{ wifi_channel_5g }}" ]; then
      uci set wireless.radio1.channel='{{ wifi_channel_5g }}'
      changed=1
    fi

    current_htmode_5="$(uci -q get wireless.radio1.htmode || true)"
    if [ "$current_htmode_5" != "{{ wifi_htmode_5g }}" ]; then
      uci set wireless.radio1.htmode='{{ wifi_htmode_5g }}'
      changed=1
    fi

    current_ssid_24="$(uci -q get wireless.default_radio0.ssid || true)"
    if [ "$current_ssid_24" != "{{ wifi_ssid }}" ]; then
      uci set wireless.default_radio0.ssid='{{ wifi_ssid }}'
      changed=1
    fi

    current_key_24="$(uci -q get wireless.default_radio0.key || true)"
    if [ "$current_key_24" != '{{ wifi_password }}' ]; then
      uci set wireless.default_radio0.key='{{ wifi_password }}'
      changed=1
    fi

    current_iface0_disabled="$(uci -q get wireless.default_radio0.disabled || true)"
    if [ "$current_iface0_disabled" != "0" ]; then
      uci set wireless.default_radio0.disabled='0'
      changed=1
    fi

    current_enc_24="$(uci -q get wireless.default_radio0.encryption || true)"
    if [ "$current_enc_24" != "sae-mixed" ]; then
      uci set wireless.default_radio0.encryption='sae-mixed'
      changed=1
    fi

    current_ssid_5="$(uci -q get wireless.default_radio1.ssid || true)"
    if [ "$current_ssid_5" != "{{ wifi_ssid }}" ]; then
      uci set wireless.default_radio1.ssid='{{ wifi_ssid }}'
      changed=1
    fi

    current_key_5="$(uci -q get wireless.default_radio1.key || true)"
    if [ "$current_key_5" != '{{ wifi_password }}' ]; then
      uci set wireless.default_radio1.key='{{ wifi_password }}'
      changed=1
    fi

    current_iface1_disabled="$(uci -q get wireless.default_radio1.disabled || true)"
    if [ "$current_iface1_disabled" != "0" ]; then
      uci set wireless.default_radio1.disabled='0'
      changed=1
    fi

    current_enc_5="$(uci -q get wireless.default_radio1.encryption || true)"
    if [ "$current_enc_5" != "sae-mixed" ]; then
      uci set wireless.default_radio1.encryption='sae-mixed'
      changed=1
    fi

    # Disable WPS to eliminate Pixie Dust / PIN brute force vulnerabilities
    [ "$(uci -q get wireless.default_radio0.wps_pushbutton || true)" = "0" ] || \
      { uci set wireless.default_radio0.wps_pushbutton='0'; changed=1; }
    [ "$(uci -q get wireless.default_radio1.wps_pushbutton || true)" = "0" ] || \
      { uci set wireless.default_radio1.wps_pushbutton='0'; changed=1; }
    [ "$(uci -q get wireless.default_radio0.wps_label || true)" = "0" ] || \
      { uci set wireless.default_radio0.wps_label='0'; changed=1; }
    [ "$(uci -q get wireless.default_radio1.wps_label || true)" = "0" ] || \
      { uci set wireless.default_radio1.wps_label='0'; changed=1; }

    if [ "$changed" -eq 1 ]; then
      uci commit wireless
      echo CHANGED
    else
      echo OK
    fi
  register: gatekeeper_wifi
  changed_when: "'CHANGED' in gatekeeper_wifi.stdout"
  no_log: true

- name: Configure Guest WiFi using GL.iNet native interfaces (guest2g/guest5g)
  ansible.builtin.raw: |
    set -eu
    changed=0

    # Guest network interface (192.168.9.1/24)
    proto="$(uci -q get network.guest.proto || true)"
    [ "$proto" = "static" ] || { uci set network.guest=interface; uci set network.guest.proto='static'; changed=1; }
    type="$(uci -q get network.guest.type || true)"
    [ "$type" = "bridge" ] || { uci set network.guest.type='bridge'; changed=1; }
    disabled="$(uci -q get network.guest.disabled || true)"
    [ "$disabled" = "0" ] || { uci set network.guest.disabled='0'; changed=1; }
    ip="$(uci -q get network.guest.ipaddr || true)"
    [ "$ip" = "192.168.9.1" ] || { uci set network.guest.ipaddr='192.168.9.1'; changed=1; }
    mask="$(uci -q get network.guest.netmask || true)"
    [ "$mask" = "255.255.255.0" ] || { uci set network.guest.netmask='255.255.255.0'; changed=1; }

    # Guest DHCP pool
    dhcp_iface="$(uci -q get dhcp.guest.interface || true)"
    [ "$dhcp_iface" = "guest" ] || { uci set dhcp.guest=dhcp; uci set dhcp.guest.interface='guest'; \
      uci set dhcp.guest.start='100'; uci set dhcp.guest.limit='150'; \
      uci set dhcp.guest.leasetime='12h'; changed=1; }

    # Guest 2.4GHz (interface nativa guest2g)
    [ "$(uci -q get wireless.guest2g.disabled || true)" = "0" ] || \
      { uci set wireless.guest2g.disabled='0'; changed=1; }
    [ "$(uci -q get wireless.guest2g.ssid || true)" = "{{ wifi_guest_ssid_24 }}" ] || \
      { uci set wireless.guest2g.ssid='{{ wifi_guest_ssid_24 }}'; changed=1; }
    [ "$(uci -q get wireless.guest2g.encryption || true)" = "sae-mixed" ] || \
      { uci set wireless.guest2g.encryption='sae-mixed'; changed=1; }
    [ "$(uci -q get wireless.guest2g.network || true)" = "guest" ] || \
      { uci set wireless.guest2g.network='guest'; changed=1; }
    [ "$(uci -q get wireless.guest2g.isolate || true)" = "1" ] || \
      { uci set wireless.guest2g.isolate='1'; changed=1; }
    [ "$(uci -q get wireless.guest2g.key || true)" = '{{ wifi_guest_password }}' ] || \
      { uci set wireless.guest2g.key='{{ wifi_guest_password }}'; changed=1; }

    # Guest 5GHz (interface nativa guest5g)
    [ "$(uci -q get wireless.guest5g.disabled || true)" = "0" ] || \
      { uci set wireless.guest5g.disabled='0'; changed=1; }
    [ "$(uci -q get wireless.guest5g.ssid || true)" = "{{ wifi_guest_ssid_5 }}" ] || \
      { uci set wireless.guest5g.ssid='{{ wifi_guest_ssid_5 }}'; changed=1; }
    [ "$(uci -q get wireless.guest5g.encryption || true)" = "sae-mixed" ] || \
      { uci set wireless.guest5g.encryption='sae-mixed'; changed=1; }
    [ "$(uci -q get wireless.guest5g.network || true)" = "guest" ] || \
      { uci set wireless.guest5g.network='guest'; changed=1; }
    [ "$(uci -q get wireless.guest5g.isolate || true)" = "1" ] || \
      { uci set wireless.guest5g.isolate='1'; changed=1; }
    [ "$(uci -q get wireless.guest5g.key || true)" = '{{ wifi_guest_password }}' ] || \
      { uci set wireless.guest5g.key='{{ wifi_guest_password }}'; changed=1; }

    # Remover interfaces duplicadas criadas anteriormente
    if uci -q get wireless.guest_radio0 >/dev/null 2>&1; then
      uci delete wireless.guest_radio0; changed=1
    fi
    if uci -q get wireless.guest_radio1 >/dev/null 2>&1; then
      uci delete wireless.guest_radio1; changed=1
    fi

    if [ "$changed" -eq 1 ]; then
      uci commit network
      uci commit dhcp
      uci commit wireless
      echo CHANGED
    else
      echo OK
    fi
  register: gatekeeper_guest_wifi
  changed_when: "'CHANGED' in gatekeeper_guest_wifi.stdout"
  no_log: true

- name: Configure VLAN 9 isolation for corporate work port and trunking
  ansible.builtin.raw: |
    set -eu
    changed=0

    # Ensure internal switch VLAN 1 (LAN) contains Port 1 (to UniFi switch) and CPU (5t)
    cur_lan_ports="$(uci -q get network.vlan_lan.ports || true)"
    if [ "$cur_lan_ports" != "1 5t" ]; then
      uci set network.vlan_lan.ports='1 5t'
      changed=1
    fi

    # Configure VLAN 9 for Corporate Work: Port 2 (untagged on router) and Port 1t / 5t (tagged)
    cur_work_vlan="$(uci -q get network.vlan_work.vlan || true)"
    cur_work_ports="$(uci -q get network.vlan_work.ports || true)"
    if [ "$cur_work_vlan" != "{{ work_vlan_id }}" ] || [ "$cur_work_ports" != "1t 2 5t" ]; then
      uci -q delete network.vlan_work || true
      uci set network.vlan_work=switch_vlan
      uci set network.vlan_work.device='switch0'
      uci set network.vlan_work.vlan='{{ work_vlan_id }}'
      uci set network.vlan_work.ports='1t 2 5t'
      changed=1
    fi

    # Bridge eth0.9 into the guest network interface
    cur_guest_ifname="$(uci -q get network.guest.ifname || true)"
    if ! echo "$cur_guest_ifname" | grep -q "eth0.{{ work_vlan_id }}"; then
      uci set network.guest.ifname="guest eth0.{{ work_vlan_id }}"
      changed=1
    fi

    [ "$changed" -eq 1 ] && uci commit network
    [ "$changed" -eq 1 ] && echo CHANGED || echo OK
  register: gatekeeper_work_vlan
  changed_when: "'CHANGED' in gatekeeper_work_vlan.stdout"

- name: Add reverse route for Tailscale subnet via Pi gateway
  ansible.builtin.raw: |
    set -eu
    changed=0

    if ! uci -q get network.tailscale_reverse_route >/dev/null 2>&1; then
      uci set network.tailscale_reverse_route='route'
      changed=1
    fi

    current_target="$(uci -q get network.tailscale_reverse_route.target || true)"
    if [ "$current_target" != "100.64.0.0" ]; then
      uci set network.tailscale_reverse_route.target='100.64.0.0'
      changed=1
    fi

    current_mask="$(uci -q get network.tailscale_reverse_route.netmask || true)"
    if [ "$current_mask" != "255.192.0.0" ]; then
      uci set network.tailscale_reverse_route.netmask='255.192.0.0'
      changed=1
    fi

    current_gateway="$(uci -q get network.tailscale_reverse_route.gateway || true)"
    if [ "$current_gateway" != "{{ pi_tailscale_ip }}" ]; then
      uci set network.tailscale_reverse_route.gateway='{{ pi_tailscale_ip }}'
      changed=1
    fi

    current_interface="$(uci -q get network.tailscale_reverse_route.interface || true)"
    if [ "$current_interface" != "lan" ]; then
      uci set network.tailscale_reverse_route.interface='lan'
      changed=1
    fi

    if [ "$changed" -eq 1 ]; then
      echo CHANGED
    else
      echo OK
    fi
  register: gatekeeper_route
  changed_when: "'CHANGED' in gatekeeper_route.stdout"

- name: Check Pi (AdGuard Home) reachability
  ansible.builtin.raw: ping -c1 -W2 {{ pi_adguard_ip }} >/dev/null 2>&1 && echo REACHABLE || echo UNREACHABLE
  register: gatekeeper_pi_check
  changed_when: false
  check_mode: false

- name: Force all LAN DNS traffic to designated resolver (AdGuard when Pi is reachable, local Opal DoT when Pi is down)
  ansible.builtin.raw: |
    set -eu
    changed=0
    pi_reachable="{{ 'yes' if (gatekeeper_pi_check.stdout | default('')) | trim == 'REACHABLE' else 'no' }}"

    if [ "$pi_reachable" = "yes" ]; then
      target_ip="{{ pi_adguard_ip }}"
    else
      target_ip="192.168.8.1"
    fi

    if ! uci -q get firewall.dns_hijack >/dev/null 2>&1; then
      uci set firewall.dns_hijack='redirect'
      changed=1
    fi

    [ "$(uci -q get firewall.dns_hijack.name || true)" = "Force-DNS-Hijack" ] || \
      { uci set firewall.dns_hijack.name='Force-DNS-Hijack'; changed=1; }

    [ "$(uci -q get firewall.dns_hijack.src || true)" = "lan" ] || \
      { uci set firewall.dns_hijack.src='lan'; changed=1; }

    [ "$(uci -q get firewall.dns_hijack.proto || true)" = "tcp udp" ] || \
      { uci set firewall.dns_hijack.proto='tcp udp'; changed=1; }

    [ "$(uci -q get firewall.dns_hijack.src_dport || true)" = "53" ] || \
      { uci set firewall.dns_hijack.src_dport='53'; changed=1; }

    if uci -q get firewall.dns_hijack.dest >/dev/null 2>&1; then
      uci delete firewall.dns_hijack.dest
      changed=1
    fi

    [ "$(uci -q get firewall.dns_hijack.dest_ip || true)" = "$target_ip" ] || \
      { uci set firewall.dns_hijack.dest_ip="$target_ip"; changed=1; }

    [ "$(uci -q get firewall.dns_hijack.dest_port || true)" = "53" ] || \
      { uci set firewall.dns_hijack.dest_port='53'; changed=1; }

    if uci -q get firewall.dns_hijack_adguard >/dev/null 2>&1; then
      uci delete firewall.dns_hijack_adguard
      changed=1
    fi

    [ "$changed" -eq 1 ] && uci commit firewall
    [ "$changed" -eq 1 ] && echo CHANGED || echo OK
  register: gatekeeper_dns
  changed_when: "'CHANGED' in gatekeeper_dns.stdout"

- name: Configure Stubby DNS-over-TLS with Family Protection (Cloudflare + CleanBrowsing + AdGuard Family)
  ansible.builtin.raw: |
    set -eu
    changed=0

    cur_enabled="$(uci -q get stubby.global.enabled || true)"
    if [ "$cur_enabled" != "1" ]; then
      uci set stubby.global.enabled='1'
      changed=1
    fi

    cur_primary="$(uci -q get stubby.@resolver[0].address || true)"
    cur_fallback="$(uci -q get stubby.@resolver[2].address || true)"
    if [ "$cur_primary" != "1.1.1.3" ] || [ "$cur_fallback" != "185.228.168.168" ]; then
      while uci -q delete stubby.@resolver[0]; do :; done
      uci add stubby resolver
      uci set stubby.@resolver[-1].address='1.1.1.3'
      uci set stubby.@resolver[-1].tls_auth_name='family.cloudflare-dns.com'
      uci add stubby resolver
      uci set stubby.@resolver[-1].address='1.0.0.3'
      uci set stubby.@resolver[-1].tls_auth_name='family.cloudflare-dns.com'
      uci add stubby resolver
      uci set stubby.@resolver[-1].address='185.228.168.168'
      uci set stubby.@resolver[-1].tls_auth_name='family-filter-dns.cleanbrowsing.org'
      uci add stubby resolver
      uci set stubby.@resolver[-1].address='94.140.14.15'
      uci set stubby.@resolver[-1].tls_auth_name='family.adguard-dns.com'
      changed=1
    fi

    if [ "$changed" -eq 1 ]; then
      uci commit stubby
      /etc/init.d/stubby enable
      /etc/init.d/stubby restart
      echo CHANGED
    else
      if ! ps | grep -v grep | grep -q stubby; then
        /etc/init.d/stubby start
        echo CHANGED
      else
        echo OK
      fi
    fi
  register: gatekeeper_stubby
  changed_when: "'CHANGED' in gatekeeper_stubby.stdout"

- name: Configure dnsmasq DNS protection and upstream
  ansible.builtin.raw: |
    set -eu
    changed=0
    pi_reachable="{{ 'yes' if (gatekeeper_pi_check.stdout | default('')) | trim == 'REACHABLE' else 'no' }}"

    # Allow 0.0.0.0 and :: sinkhole responses from family filter upstream
    cur_rebind="$(uci -q get dhcp.@dnsmasq[0].rebind_protection || true)"
    [ "$cur_rebind" = "0" ] || { uci set dhcp.@dnsmasq[0].rebind_protection='0'; changed=1; }

    # Do not use resolvfile (/tmp/resolv.conf.auto from WAN) to guarantee DoT
    cur_noresolv="$(uci -q get dhcp.@dnsmasq[0].noresolv || true)"
    [ "$cur_noresolv" = "1" ] || { uci set dhcp.@dnsmasq[0].noresolv='1'; changed=1; }

    # Super-Cache: expand DNS cache to 2000 domains and 300s min TTL for instant local resolution
    [ "$(uci -q get dhcp.@dnsmasq[0].cachesize || true)" = "2000" ] || \
      { uci set dhcp.@dnsmasq[0].cachesize='2000'; changed=1; }
    [ "$(uci -q get dhcp.@dnsmasq[0].min_cache_ttl || true)" = "300" ] || \
      { uci set dhcp.@dnsmasq[0].min_cache_ttl='300'; changed=1; }

    if [ "$pi_reachable" = "yes" ]; then
      desired="{{ pi_adguard_ip }}"
    else
      desired="{{ dot_upstream }}"
    fi
    cur="$(uci -q get dhcp.@dnsmasq[0].server || true)"
    if [ "$cur" != "$desired" ]; then
      uci -q delete dhcp.@dnsmasq[0].server || true
      uci add_list dhcp.@dnsmasq[0].server="$desired"
      changed=1
    fi

    [ "$changed" -eq 1 ] && uci commit dhcp
    [ "$changed" -eq 1 ] && echo CHANGED || echo OK
  register: gatekeeper_dnsmasq
  changed_when: "'CHANGED' in gatekeeper_dnsmasq.stdout"

- name: Configure static DHCP reservation for UniFi Switch
  ansible.builtin.raw: |
    set -eu
    changed=0

    cur_ip="$(uci -q get dhcp.unifi_switch.ip || true)"
    cur_mac="$(uci -q get dhcp.unifi_switch.mac || true)"
    if [ "$cur_ip" != "{{ switch_unifi_ip }}" ] || [ "$cur_mac" != "{{ switch_unifi_mac }}" ]; then
      uci -q delete dhcp.unifi_switch || true
      uci set dhcp.unifi_switch=host
      uci set dhcp.unifi_switch.name='{{ switch_unifi_name }}'
      uci set dhcp.unifi_switch.mac='{{ switch_unifi_mac }}'
      uci set dhcp.unifi_switch.ip='{{ switch_unifi_ip }}'
      changed=1
    fi

    [ "$changed" -eq 1 ] && uci commit dhcp
    [ "$changed" -eq 1 ] && echo CHANGED || echo OK
  register: gatekeeper_switch_dhcp
  changed_when: "'CHANGED' in gatekeeper_switch_dhcp.stdout"

- name: Configure WAN interface with static IP for DMZ (Technicolor upstream)
  ansible.builtin.raw: |
    set -eu
    changed=0

    current_proto="$(uci -q get network.wan.proto || true)"
    if [ "$current_proto" != "static" ]; then
      uci set network.wan.proto='static'
      changed=1
    fi

    current_ip="$(uci -q get network.wan.ipaddr || true)"
    if [ "$current_ip" != "{{ wan_static_ip }}" ]; then
      uci set network.wan.ipaddr='{{ wan_static_ip }}'
      changed=1
    fi

    current_mask="$(uci -q get network.wan.netmask || true)"
    if [ "$current_mask" != "255.255.255.0" ]; then
      uci set network.wan.netmask='255.255.255.0'
      changed=1
    fi

    current_gw="$(uci -q get network.wan.gateway || true)"
    if [ "$current_gw" != "{{ wan_gateway }}" ]; then
      uci set network.wan.gateway='{{ wan_gateway }}'
      changed=1
    fi

    current_dns="$(uci -q get network.wan.dns || true)"
    if [ "$current_dns" != "{{ wan_dns }}" ]; then
      uci set network.wan.dns='{{ wan_dns }}'
      changed=1
    fi

    if [ "$changed" -eq 1 ]; then
      echo CHANGED
    else
      echo OK
    fi
  register: gatekeeper_wan_dhcp
  changed_when: "'CHANGED' in gatekeeper_wan_dhcp.stdout"

- name: Harden WAN zone input and forward policy
  ansible.builtin.raw: |
    set -eu
    changed=0

    for zone in $(uci show firewall | sed -n "s/^firewall\.\([^=]*\)=zone$/\1/p"); do
      if uci -q show firewall.$zone | grep -Eq "network='[^']*wan([^']*)?'"; then
        current_input="$(uci -q get firewall.$zone.input || true)"
        if [ "$current_input" != "REJECT" ]; then
          uci set firewall.$zone.input='REJECT'
          changed=1
        fi

        current_forward="$(uci -q get firewall.$zone.forward || true)"
        if [ "$current_forward" != "REJECT" ]; then
          uci set firewall.$zone.forward='REJECT'
          changed=1
        fi
      fi
    done

    if [ "$changed" -eq 1 ]; then
      echo CHANGED
    else
      echo OK
    fi
  register: gatekeeper_wan
  changed_when: "'CHANGED' in gatekeeper_wan.stdout"

- name: Implement Zero Trust isolation (Guest network cannot access Admin network)
  ansible.builtin.raw: |
    set -eu
    changed=0

    # Define rule name for idempotency
    rule_name="block_guest_to_admin"

    # Check if rule already exists
    if ! uci show firewall | grep -q "name='$rule_name'"; then
      # Find the next available firewall rule index
      rule_idx=$(uci add firewall rule)

      # Configure the Zero Trust isolation rule
      uci set "firewall.$rule_idx.name=$rule_name"
      uci set "firewall.$rule_idx.src=guest"
      uci set "firewall.$rule_idx.dest=lan"
      uci set "firewall.$rule_idx.src_ip={{ guest_network_cidr }}"
      uci set "firewall.$rule_idx.dest_ip={{ network_cidr }}"
      uci set "firewall.$rule_idx.proto=all"
      uci set "firewall.$rule_idx.target=REJECT"
      uci set "firewall.$rule_idx.enabled=1"

      changed=1
      echo CHANGED
    else
      # Verify existing rule configuration
      rule_idx=$(uci show firewall | grep "name='$rule_name'" | cut -d'.' -f2 | cut -d'=' -f1)

      # Check if any settings need update
      current_target="$(uci -q get firewall.$rule_idx.target || true)"
      current_enabled="$(uci -q get firewall.$rule_idx.enabled || true)"
      current_src_ip="$(uci -q get firewall.$rule_idx.src_ip || true)"
      current_dest_ip="$(uci -q get firewall.$rule_idx.dest_ip || true)"

      if [ "$current_target" != "REJECT" ] || [ "$current_enabled" != "1" ] || \
         [ "$current_src_ip" != "{{ guest_network_cidr }}" ] || \
         [ "$current_dest_ip" != "{{ network_cidr }}" ]; then
        uci set "firewall.$rule_idx.target=REJECT"
        uci set "firewall.$rule_idx.enabled=1"
        uci set "firewall.$rule_idx.src_ip={{ guest_network_cidr }}"
        uci set "firewall.$rule_idx.dest_ip={{ network_cidr }}"
        changed=1
        echo CHANGED
      else
        echo OK
      fi
    fi
  register: gatekeeper_zero_trust
  changed_when: "'CHANGED' in gatekeeper_zero_trust.stdout"

- name: Block direct DNS-over-TLS (port 853) bypass to force devices to use network DNS
  ansible.builtin.raw: |
    set -eu
    changed=0
    rule_name="block_dot_bypass"

    if ! uci show firewall | grep -q "name='$rule_name'"; then
      uci add firewall rule >/dev/null
      rule_idx="@rule[-1]"
      uci set "firewall.$rule_idx.name=$rule_name"
      uci set "firewall.$rule_idx.src=lan"
      uci set "firewall.$rule_idx.dest=wan"
      uci set "firewall.$rule_idx.proto=tcp"
      uci set "firewall.$rule_idx.dest_port=853"
      uci set "firewall.$rule_idx.target=REJECT"
      uci set "firewall.$rule_idx.enabled=1"
      changed=1
    else
      rule_idx=$(uci show firewall | grep "name='$rule_name'" | cut -d'.' -f2 | cut -d'=' -f1)
      cur_target="$(uci -q get firewall.$rule_idx.target || true)"
      cur_port="$(uci -q get firewall.$rule_idx.dest_port || true)"
      cur_enabled="$(uci -q get firewall.$rule_idx.enabled || true)"
      if [ "$cur_target" != "REJECT" ] || [ "$cur_port" != "853" ] || [ "$cur_enabled" != "1" ]; then
        uci set "firewall.$rule_idx.target=REJECT"
        uci set "firewall.$rule_idx.dest_port=853"
        uci set "firewall.$rule_idx.enabled=1"
        changed=1
      fi
    fi

    [ "$changed" -eq 1 ] && uci commit firewall
    [ "$changed" -eq 1 ] && echo CHANGED || echo OK
  register: gatekeeper_block_dot
  changed_when: "'CHANGED' in gatekeeper_block_dot.stdout"

- name: Install Tor package
  ansible.builtin.raw: |
    if opkg list-installed 2>/dev/null | grep -q "^tor "; then
      echo OK
    elif [ -f /etc/tor/.tor_unavailable ]; then
      echo OK
    else
      mkdir -p /etc/tor
      if opkg list tor 2>/dev/null | grep -q "^tor "; then
        opkg install tor >/dev/null 2>&1 && echo CHANGED || echo OK
      else
        touch /etc/tor/.tor_unavailable
        echo OK
      fi
    fi
  register: gatekeeper_tor_install
  changed_when: "'CHANGED' in gatekeeper_tor_install.stdout"

- name: Configure Tor as transparent proxy
  ansible.builtin.raw: |
    mkdir -p /etc/tor
    tmp=$(mktemp)
    printf '%s\n' \
      'VirtualAddrNetworkIPv4 10.192.0.0/10' \
      'AutomapHostsOnResolve 1' \
      'TransPort 0.0.0.0:9040' \
      'DNSPort 0.0.0.0:5353' \
      > "$tmp"
    if cmp -s "$tmp" /etc/tor/torrc; then
      rm -f "$tmp"
      echo OK
    else
      mv "$tmp" /etc/tor/torrc
      echo CHANGED
    fi
  register: gatekeeper_tor_config
  changed_when: "'CHANGED' in gatekeeper_tor_config.stdout"

- name: Create Tor toggle script
  ansible.builtin.raw: |
    tmp=$(mktemp)
    cat > "$tmp" << 'TORSCRIPT'
    #!/bin/sh
    STATE=/var/run/tor-active
    tor_enable() {
      /etc/init.d/tor start
      sleep 3
      iptables -t nat -A PREROUTING -i br-lan -d 192.168.0.0/16 -j RETURN
      iptables -t nat -A PREROUTING -i br-lan -d 10.0.0.0/8 -j RETURN
      iptables -t nat -A PREROUTING -i br-lan -p udp --dport 53 -j REDIRECT --to-ports 5353
      iptables -t nat -A PREROUTING -i br-lan -p tcp --syn -j REDIRECT --to-ports 9040
      touch "$STATE"
      logger -t tor-toggle "Tor ENABLED - all traffic routed via Tor"
    }
    tor_disable() {
      iptables -t nat -D PREROUTING -i br-lan -d 192.168.0.0/16 -j RETURN 2>/dev/null || true
      iptables -t nat -D PREROUTING -i br-lan -d 10.0.0.0/8 -j RETURN 2>/dev/null || true
      iptables -t nat -D PREROUTING -i br-lan -p udp --dport 53 -j REDIRECT --to-ports 5353 2>/dev/null || true
      iptables -t nat -D PREROUTING -i br-lan -p tcp --syn -j REDIRECT --to-ports 9040 2>/dev/null || true
      /etc/init.d/tor stop
      rm -f "$STATE"
      logger -t tor-toggle "Tor DISABLED - normal routing restored"
    }
    [ -f "$STATE" ] && tor_disable || tor_enable
    TORSCRIPT
    if cmp -s "$tmp" /usr/bin/tor-toggle; then
      rm -f "$tmp"
      echo OK
    else
      mv "$tmp" /usr/bin/tor-toggle
      chmod +x /usr/bin/tor-toggle
      echo CHANGED
    fi
  register: gatekeeper_tor_script
  changed_when: "'CHANGED' in gatekeeper_tor_script.stdout"

- name: Create GL.iNet native Tor switch script
  ansible.builtin.raw: |
    mkdir -p /etc/gl-switch.d
    tmp=$(mktemp)
    cat > "$tmp" << 'TORSH'
    #!/bin/sh
    case "$1" in
      on)
        /etc/init.d/tor start
        sleep 3
        iptables -t nat -A PREROUTING -i br-lan -d 192.168.0.0/16 -j RETURN
        iptables -t nat -A PREROUTING -i br-lan -d 10.0.0.0/8 -j RETURN
        iptables -t nat -A PREROUTING -i br-lan -p udp --dport 53 -j REDIRECT --to-ports 5353
        iptables -t nat -A PREROUTING -i br-lan -p tcp --syn -j REDIRECT --to-ports 9040
        logger -t gl-switch "Tor ENABLED - all traffic via Tor"
        ;;
      off)
        iptables -t nat -D PREROUTING -i br-lan -d 192.168.0.0/16 -j RETURN 2>/dev/null || true
        iptables -t nat -D PREROUTING -i br-lan -d 10.0.0.0/8 -j RETURN 2>/dev/null || true
        iptables -t nat -D PREROUTING -i br-lan -p udp --dport 53 -j REDIRECT --to-ports 5353 2>/dev/null || true
        iptables -t nat -D PREROUTING -i br-lan -p tcp --syn -j REDIRECT --to-ports 9040 2>/dev/null || true
        /etc/init.d/tor stop
        logger -t gl-switch "Tor DISABLED - normal routing restored"
        ;;
    esac
    TORSH
    if cmp -s "$tmp" /etc/gl-switch.d/tor.sh; then
      rm -f "$tmp"
      echo OK
    else
      mv "$tmp" /etc/gl-switch.d/tor.sh
      chmod +x /etc/gl-switch.d/tor.sh
      echo CHANGED
    fi
  register: gatekeeper_tor_button
  changed_when: "'CHANGED' in gatekeeper_tor_button.stdout"

- name: Configure GL.iNet switch button function to Tor
  ansible.builtin.raw: |
    set -eu
    changed=0
    # switch-button is GL.iNet-specific; skip gracefully on standard OpenWrt
    uci -q show switch-button >/dev/null 2>&1 || { echo OK; exit 0; }
    current="$(uci -q get switch-button.@main[0].func || true)"
    if [ "$current" != "tor" ]; then
      uci set switch-button.@main[0].func='tor'
      uci commit switch-button
      changed=1
    fi
    [ "$changed" -eq 1 ] && echo CHANGED || echo OK
  register: gatekeeper_tor_switch_uci
  changed_when: "'CHANGED' in gatekeeper_tor_switch_uci.stdout"

- name: Ensure Tor does not start automatically on boot
  ansible.builtin.raw: |
    if [ -x /etc/init.d/tor ]; then
      if ls /etc/rc.d/S[0-9][0-9]tor >/dev/null 2>&1; then
        /etc/init.d/tor disable
        echo CHANGED
      else
        echo OK
      fi
    else
      echo OK
    fi
  register: gatekeeper_tor_autostart
  changed_when: "'CHANGED' in gatekeeper_tor_autostart.stdout"

- name: Disable and stop unnecessary services
  ansible.builtin.raw: |
    set -eu
    changed=0

    for svc in gl_tertf gl_health gl_macclone samba uhttpd sms_manager smstools3 repeater gl-cloud gl-alarm; do
      if [ -x "/etc/init.d/$svc" ]; then
        if ls /etc/rc.d/S*"$svc" >/dev/null 2>&1; then
          /etc/init.d/$svc disable
          changed=1
        fi

        /etc/init.d/$svc stop >/dev/null 2>&1 || true
      fi
    done

    if [ "$changed" -eq 1 ]; then
      echo CHANGED
    else
      echo OK
    fi
  register: gatekeeper_services
  changed_when: "'CHANGED' in gatekeeper_services.stdout"

- name: Configure zram-swap for compressed RAM memory safety buffer
  ansible.builtin.raw: |
    set -eu
    changed=0

    if ! opkg list-installed 2>/dev/null | grep -q "^zram-swap "; then
      opkg update >/dev/null 2>&1
      opkg install zram-swap >/dev/null 2>&1
      changed=1
    fi

    if [ -x /etc/init.d/zram ]; then
      if ! ls /etc/rc.d/S*zram >/dev/null 2>&1; then
        /etc/init.d/zram enable
        changed=1
      fi
      if ! swapon -s 2>/dev/null | grep -q "zram"; then
        /etc/init.d/zram start
        changed=1
      fi
    fi

    [ "$changed" -eq 1 ] && echo CHANGED || echo OK
  register: gatekeeper_zram
  changed_when: "'CHANGED' in gatekeeper_zram.stdout"

- name: Configure kernel sysctl network latency and security tuning
  ansible.builtin.raw: |
    set -eu
    tmp=$(mktemp)
    cat > "$tmp" << 'SYSCTL'
    # Network performance and low-latency tuning
    net.ipv4.tcp_fastopen=3
    net.ipv4.tcp_slow_start_after_idle=0
    net.ipv4.tcp_tw_reuse=1
    # Security and spoofing defenses
    net.ipv4.icmp_echo_ignore_broadcasts=1
    net.ipv4.icmp_ignore_bogus_error_responses=1
    net.ipv4.conf.all.rp_filter=1
    net.ipv4.conf.default.rp_filter=1
    SYSCTL
    if cmp -s "$tmp" /etc/sysctl.d/99-performance.conf; then
      rm -f "$tmp"
      echo OK
    else
      mv "$tmp" /etc/sysctl.d/99-performance.conf
      sysctl -p /etc/sysctl.d/99-performance.conf >/dev/null 2>&1 || true
      echo CHANGED
    fi
  register: gatekeeper_sysctl
  changed_when: "'CHANGED' in gatekeeper_sysctl.stdout"

- name: Configure automated periodic memory cache trimming
  ansible.builtin.raw: |
    set -eu
    mkdir -p /etc/periodic/hourly
    tmp=$(mktemp)
    cat > "$tmp" << 'MEMTRIM'
    #!/bin/sh
    # Release inactive slab and page caches if free RAM drops below 15MB
    free_kb=$(awk '/MemAvailable/ {print $2}' /proc/meminfo 2>/dev/null || awk '/MemFree/ {print $2}' /proc/meminfo)
    if [ "${free_kb:-99999}" -lt 15360 ]; then
      sync
      echo 3 > /proc/sys/vm/drop_caches
      logger -t memory-trim "Cleaned page cache: free RAM was below 15MB"
    fi
    MEMTRIM
    if cmp -s "$tmp" /etc/periodic/hourly/memory-trim; then
      rm -f "$tmp"
      echo OK
    else
      mv "$tmp" /etc/periodic/hourly/memory-trim
      chmod +x /etc/periodic/hourly/memory-trim
      echo CHANGED
    fi
  register: gatekeeper_memtrim
  changed_when: "'CHANGED' in gatekeeper_memtrim.stdout"

- name: Configure automatic LED schedule (turn off 20:00, turn on 09:00 São Paulo time)
  ansible.builtin.raw: |
    set -eu
    changed=0

    cur_enable="$(uci -q get gl_timer.led.enable || true)"
    if [ "$cur_enable" != "1" ]; then
      uci set gl_timer.led.enable='1'
      changed=1
    fi

    cur_off_h="$(uci -q get gl_timer.led.turnoff_hour || true)"
    if [ "$cur_off_h" != "{{ led_turnoff_hour }}" ]; then
      uci set gl_timer.led.turnoff_hour='{{ led_turnoff_hour }}'
      changed=1
    fi

    cur_off_m="$(uci -q get gl_timer.led.turnoff_min || true)"
    if [ "$cur_off_m" != "{{ led_turnoff_min }}" ]; then
      uci set gl_timer.led.turnoff_min='{{ led_turnoff_min }}'
      changed=1
    fi

    cur_on_h="$(uci -q get gl_timer.led.turnon_hour || true)"
    if [ "$cur_on_h" != "{{ led_turnon_hour }}" ]; then
      uci set gl_timer.led.turnon_hour='{{ led_turnon_hour }}'
      changed=1
    fi

    cur_on_m="$(uci -q get gl_timer.led.turnon_min || true)"
    if [ "$cur_on_m" != "{{ led_turnon_min }}" ]; then
      uci set gl_timer.led.turnon_min='{{ led_turnon_min }}'
      changed=1
    fi

    if [ "$changed" -eq 1 ]; then
      uci commit gl_timer
      /etc/init.d/gl_timer restart
      # If current local time is in the night window (20:00 to 09:00), turn off immediately
      current_hour=$(date +%H)
      if [ "$current_hour" -ge 20 ] || [ "$current_hour" -lt 9 ]; then
        /etc/init.d/gl_led turnoff
      else
        /etc/init.d/gl_led turnon
      fi
      echo CHANGED
    else
      echo OK
    fi
  register: gatekeeper_led_schedule
  changed_when: "'CHANGED' in gatekeeper_led_schedule.stdout"

- name: Commit UCI changes and restart network and firewall
  ansible.builtin.raw: |
    uci commit
    /etc/init.d/network restart
    /etc/init.d/firewall restart
    /etc/init.d/dnsmasq restart
    wifi down && sleep 2 && wifi up
    echo CHANGED
  register: gatekeeper_commit
  changed_when: "'CHANGED' in gatekeeper_commit.stdout"
  when:
    - gatekeeper_perf is changed
      or gatekeeper_wifi is changed
      or gatekeeper_guest_wifi is changed
      or gatekeeper_work_vlan is changed
      or gatekeeper_switch_dhcp is changed
      or gatekeeper_route is changed
      or gatekeeper_dns is changed
      or gatekeeper_stubby is changed
      or gatekeeper_dnsmasq is changed
      or gatekeeper_wan_dhcp is changed
      or gatekeeper_wan is changed
      or gatekeeper_block_dot is changed
      or gatekeeper_services is changed

- name: Clean up legacy Force-DNS-to-AdGuard iptables rules if present
  ansible.builtin.raw: |
    chain="zone_lan_prerouting"
    changed=0
    iptables -t nat -S "$chain" 2>/dev/null | \
      grep 'Force-DNS-to-AdGuard' | \
      sed "s/-A $chain/-D $chain/" | \
      while IFS= read -r rule; do
        iptables -t nat $rule 2>/dev/null && changed=1
      done
    [ "$changed" -eq 1 ] && echo CHANGED || echo OK
  register: gatekeeper_dns_iptables
  changed_when: "'CHANGED' in gatekeeper_dns_iptables.stdout"
